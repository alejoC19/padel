'use client';

import { PlayerUnifiedBookingsScreen } from '@/components/player/PlayerUnifiedBookingsScreen';

/** `/jugador/mis-reservas` — reservas del jugador en cualquier club de la plataforma. */
export default function JugadorMisReservasRoute() {
  return <PlayerUnifiedBookingsScreen />;
}
