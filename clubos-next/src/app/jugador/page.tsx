'use client';

import { PlayerDirectoryScreen } from '@/components/player/PlayerDirectoryScreen';

/** `/jugador` — directorio: encontrá tu club entre todos los de la plataforma. */
export default function JugadorRoute() {
  return <PlayerDirectoryScreen />;
}
